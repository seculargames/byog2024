import {gameState,buildingOpts } from './stores.ts'

/*function findPoss(usedPoss) {
  // find a new postition that is not occupied by prev. buildings
  let overlap=true;
  while(ovelap) {
    xpos = Math.floor(Math.random() * canvasSize[0]);
    ypos = Math.floor(Math.random() * canvasSize[1]);
    if not (xpos,ypos) in usedPoss) {
      overlap=false;
    }

  }
  return poss
} */

export const updateGameState = function(building, ) {

}
